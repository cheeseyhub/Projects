//Math methods and properties

console.log(Math.PI); //gives the value of pi

console.log(Math.trunc(Math.PI)); //removes decimals to a integer

console.log(Math.round(Math.PI)); //returns value rounded to the nearest interger

console.log(Math.ceil(Math.PI)); //Always rounds off the value to higher remember it by ceiling

console.log(Math.floor(Math.PI)); //Alwats rounds down the value to lower number

console.log(Math.pow(2, 3)); //takes two values first is the number second is the exonent)

console.log(Math.min(2, 4, 5)); //Returns the smallest interger from given numbers seperate numbers with commas

console.log(Math.max(2, 4, 5)); //returns the largest interger from given numbers

console.log(Math.random()); //returns a random number between 0 and 1 does not include interger one

//expression for returning number between 1 to 10
console.log(Math.floor(math.random() * 10) + 1);
